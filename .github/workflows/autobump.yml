# brew tap-new uses this file to generate an autobump workflow for the new tap
name: brew bump

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/autobump.yml
  schedule:
    # Every day at 11:30 UTC
    - cron: 30 11 * * *
  # NOTE: added `workflow_dispatch` event for debugging
  workflow_dispatch:

permissions: {}

defaults:
  run:
    shell: bash -xeuo pipefail {0}

jobs:
  autobump:
    # NOTE: bumping macOS-only casks needs to run on macOS
    runs-on: macos-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@f8d4222eb633e65c2a0157383cf80861fc7fae7f # 2026.09.07.1

      - name: Set up git
        id: git_setup
        uses: Homebrew/actions/git-user-config@f8d4222eb633e65c2a0157383cf80861fc7fae7f # 2026.09.07.1

      - name: Bump casks
        # `brew bump` may fail if the same-named feature branch already
        # exists, which can happen when pushing to `main` while there are
        # open version bump PRs.
        continue-on-error: ${{ github.event_name == 'push' }}
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.HOMEBREW_GITHUB_API_TOKEN || secrets.GITHUB_TOKEN }}
          HOMEBREW_GIT_COMMITTER_NAME: ${{ steps.git_setup.outputs.name }}
          HOMEBREW_GIT_COMMITTER_EMAIL: ${{ steps.git_setup.outputs.email }}
          TAP_NAME: ${{ steps.set-up-homebrew.outputs.tap-name }}
        # NOTE: option `--full-name` is needed to avoid resolving to the same
        # named cask in core `Homebrew/cask` tap
        # see https://github.com/orgs/Homebrew/discussions/7015#discussioncomment-18056484
        # brew bump docs https://docs.brew.sh/Manpage#bump-options-formulacask-
        run: brew bump --no-fork --open-pr --casks --full-name --tap="$TAP_NAME"
